<Window x:Class="StockfishCompiler.Views.DarkMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        AllowsTransparency="True"
        MinWidth="320"
        MaxWidth="520"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown"
        Title="Message">
    <Border x:Name="DialogBorder"
            Background="{StaticResource SecondaryBrush}"
            BorderBrush="{StaticResource AccentBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Rectangle x:Name="AccentStripe"
                       Grid.Column="0"
                       RadiusX="4"
                       RadiusY="4"
                       Fill="{StaticResource AccentBrush}"
                       Margin="0,0,0,0"/>

            <Grid Grid.Column="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Vertical" Margin="4,0,4,12">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Border x:Name="SeverityBadge"
                                Background="{StaticResource AccentBrush}"
                                CornerRadius="4"
                                Padding="8,4"
                                Visibility="Collapsed"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="SeverityBadgeText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock x:Name="TitleText"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource AccentBrush}"
                                   Margin="8,0,0,0"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBox x:Name="MessageTextBox"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             IsReadOnly="True"
                             Background="{StaticResource SecondaryBrush}"
                             BorderThickness="0"
                             Foreground="{StaticResource TextSecondaryBrush}"
                             Padding="0"
                             CaretBrush="{StaticResource TextSecondaryBrush}"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Top"
                             MinHeight="24"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CopyButton"
                            Content="Copy"
                            Click="CopyButton_Click"
                            Style="{StaticResource DarkButton}"
                            Padding="12,6"
                            MinWidth="80"
                            Margin="0,0,8,0"/>
                    <Button x:Name="PrimaryButton"
                            Click="PrimaryButton_Click"
                            Style="{StaticResource DarkButton}"
                            Padding="12,6"
                            MinWidth="80"/>
                    <Button x:Name="SecondaryButton"
                            Click="SecondaryButton_Click"
                            Style="{StaticResource DarkButton}"
                            Padding="12,6"
                            MinWidth="80"
                            Margin="8,0,0,0"
                            Visibility="Collapsed"/>
                    <Button x:Name="TertiaryButton"
                            Click="TertiaryButton_Click"
                            Style="{StaticResource DarkButton}"
                            Padding="12,6"
                            MinWidth="80"
                            Margin="8,0,0,0"
                            Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
