<Window x:Class="StockfishCompiler.Views.CompilerInstallerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="C++ Compiler Required" 
        Height="500" 
        Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="C++ Compiler Required" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="{DynamicResource ForegroundBrush}"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" 
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource ForegroundMutedBrush}"
                   Margin="0,0,0,15">
            <Run Text="To compile Stockfish, you need MSYS2 with a C++ compiler installed."/>
            <LineBreak/>
            <Run Text="Click 'Show Instructions' to see the installation steps."/>
        </TextBlock>

        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,15">
            <TextBox x:Name="LogTextBox"
                     Style="{StaticResource DarkTextBox}"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="12"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     MinHeight="250"/>
        </ScrollViewer>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="InstallButton"
                    Content="Show Instructions"
                    MinWidth="140"
                    Height="35"
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Style="{StaticResource DarkButton}"
                    Click="InstallButton_Click"/>

            <Button x:Name="CancelButton"
                    Content="Close"
                    MinWidth="90"
                    Height="35"
                    Padding="15,5"
                    Style="{StaticResource DarkButton}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
