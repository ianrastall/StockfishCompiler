name: Build Stockfish

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Build Tools
        run: sudo apt-get update && sudo apt-get install -y make g++

      - name: Compile Stockfish
        # CHANGED: Removed 'cd src' to build from the repository root
        run: make -j $(nproc) build ARCH=x86-64

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: stockfish-executable
          # CHANGED: Updated path to look for the file in root, not src/
          path: stockfish 
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: stockfish-executable

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          # CHANGED: Updated path here as well
          files: stockfish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}